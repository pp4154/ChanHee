# 감염자 수 예측 프로그램

def calculate_infected(P0, R, t):
    return P0 * (R ** t)

# 사용자 입력
P0 = int(input("현재 감염자 수를 입력하세요: "))  # 초기 감염자 수
R = float(input("감염 재생산 수를 입력하세요: "))  # 감염 재생산 수
t = int(input("예측할 기간(일)을 입력하세요: "))  # 예측 일수

# 감염자 수 계산
infected = calculate_infected(P0, R, t)

# 결과 출력
print(f"{t}일 후 예상 감염자 수는 {infected:.1f}명입니다.")
0f} 원")
